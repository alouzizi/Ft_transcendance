import React from 'react';

const SettingPage = async ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            {children}
        </div>
    );
};
export default SettingPage;
